// VIRE Portal - Alpine.js components
// This file registers Alpine.js components for interactive UI elements.
// Alpine.js is loaded from CDN (no build step required).

document.addEventListener('alpine:init', () => {
    // Future Alpine.js components will be registered here.
    // Example: Alpine.data('settings', () => ({ ... }))
});
