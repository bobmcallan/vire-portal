# VIRE Portal Configuration (local dev)
# Priority: defaults < this file < environment variables (VIRE_*) < CLI flags
#
# Copy to vire-portal.toml and edit:
#   cp config/vire-portal.toml.example config/vire-portal.toml

environment = "dev"

[server]
port = 4241
host = "localhost"

[api]
url = "http://localhost:4242"

[auth]
jwt_secret = ""
callback_url = "http://localhost:4241/auth/callback"
portal_url = ""    # Leave empty for local dev (derived from host:port). Set for tunnel/prod.

[user]
# portfolios = ["SMSF", "Personal"]
# display_currency = "AUD"

[logging]
level = "info"              # debug, info, warn, error
format = "text"             # text, json
outputs = ["console", "file"]  # console (stderr), file
file_path = "logs/vire-portal.log"
max_size_mb = 10
max_backups = 3
