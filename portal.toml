# VIRE Portal Configuration
# Priority: defaults < this file < environment variables (VIRE_*) < CLI flags

[server]
port = 8080
host = "localhost"

[storage.badger]
path = "./data/vire"

[logging]
level = "info"    # debug, info, warn, error
format = "text"   # text, json
